[project]
name = "razoruap"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn>=0.28.0",
    "pydantic>=2.6.0",
    "razorpay>=1.4.1",
    "requests>=2.31.0",
    "python-dotenv>=1.0.1",
]

[tool.vercel]
entrypoint = "backend.main:app"